// 配置
const config = {
  // 启动端口
  port: 8090,
  // 数据库配置
  database: {
    HOST: '39.106.8.226',
    PORT: '3306',
    DATABASE: 'yoshop',
    USERNAME: 'root',
    PASSWORD: 'cainiao'
  }
}

module.exports = config
